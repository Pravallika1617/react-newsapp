import React from 'react'
import spinner from './images/spinner.gif'

function Loader() {
    return (
        <div>
            <img scr={spinner} alt='' />
        </div>
    )
}

export default Loader
